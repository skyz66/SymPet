{% extends 'base.html.twig' %}
{% block title %}Edit Category — Admin{% endblock %}
{% block body %}
<div class="admin-shell" style="max-width: 820px; margin: auto;">
    <div class="d-flex justify-content-between align-items-center mb-4"><div><div class="eyebrow">Catalog</div><h1 class="display-6 fw-bold mb-0">Edit Category</h1></div><a href="{{ path('app_admin_category_index') }}" class="btn btn-outline-secondary">Back to list</a></div>
    {{ include('admin_category/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="mt-3">{{ include('admin_category/_delete_form.html.twig') }}</div>
</div>
{% endblock %}
